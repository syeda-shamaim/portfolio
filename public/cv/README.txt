Place your CV PDF file here and name it `cv.pdf`.

The portfolio will offer a "Download CV" button that serves this file.

To use a different filename, update `cv` and `cvFileName` in `src/data/portfolio.ts`.
